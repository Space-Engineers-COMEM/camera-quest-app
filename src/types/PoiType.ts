export default interface PoiType {
  id: string;
  azure_tag: string;
  exhibition_number: number;
  title: string;
  author: string;
  periode: string;
  visible: boolean;
  description: string;
  origin: string;
}
