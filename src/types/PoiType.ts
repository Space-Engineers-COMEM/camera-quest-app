export default interface PoiType {
  id: number;
  azure_tag: string;
  exhibition_number: number;
  title: string;
  author: string;
  periode: string;
  visible: boolean;
  area: number;
  description: string;
  origin: string;
  image_url: string;
}
